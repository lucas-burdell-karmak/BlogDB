@using (Html.BeginForm("SearchResult", "Home"))
{
    <div class="form-group">
        @Html.TextBoxFor(m => m.Author, new {@class="form-control", type="search", placeholder="Search..."})
        <button class="btn-default">Search</button>
    </div>

}
