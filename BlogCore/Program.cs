using System;
using System.Collections.Generic;
using BlogDB.Core;

namespace BlogDB
{
    class Program
    {
        static void Main(string[] args)
        {
            // SQLPostRepo mysqlDB = new SQLPostRepo();
            // var list = new List<Post>();
            // var post = new Post("I Like To Code", "Drew Hans", "This is my body");
            // list.Add(post);
            // mysqlDB.WriteAll(list);

            // var returnedList = mysqlDB.ReadAll();

            // foreach(Post p in returnedList) {
            //     Console.WriteLine(p.Title);
            //     Console.WriteLine(p.Author);
            //     Console.WriteLine(p.Body);
            // }
        }
    }
}
